#!/usr/bin/env bash
# This script displays all active IPv4 IP addresses on the machine.

# Use the ip command to list all IP addresses and filter out only the active IPv4 addresses
ip -4 addr show | grep -oP '(?<=inet\s)\d+(\.\d+){3}'
