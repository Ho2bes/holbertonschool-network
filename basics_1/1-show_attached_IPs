#!/usr/bin/env bash
# Displays the active IPv4 addresses on the current system using ifconfig

# Extract and display active IPv4 addresses
ifconfig | grep 'inet ' | awk '{print $2}' | cut -d ':' -f 2
